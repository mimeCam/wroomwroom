`*-z` files are examples. To use z.ai set valid API_KEY.

To add a supported inference provider or use secondary subscription:
- create new `openloop_cc_docker-%X%` file
- set API_KEY & API_ENDPOINT, or unset to use subscription
