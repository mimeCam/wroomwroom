#!/usr/bin/env bash
#
# ya-swiftbridge - yolo-anthropic (one-shot): Claude code with Anthropic, swiftbridge devcontainer
#

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

PROMPT="$1"
if [ -z "$PROMPT" ]; then
    echo "Error: PROMPT is required" >&2
    echo "Usage: $0 \"your prompt here\"" >&2
    exit 1
fi

CONTAINER_NAME="ya-swiftbridge"
IMAGE="yolo_devcontainer:swiftbridge"
source "$SCRIPT_DIR/y"
